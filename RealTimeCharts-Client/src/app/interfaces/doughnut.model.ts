export interface Doughnut {
    data: any[],
    label: string,
    backgroundColor: string;
}

export interface DoughnutBetha {
    data: number[],
    labels: string[],
    backgroundColors: string[];
}